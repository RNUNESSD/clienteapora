= link_to 'Editar', edit_resource_path(r), class: "btn btn-info btn-sm" if can? :update, resource_class
= link_to 'Remover', resource_path(r), method: :delete, class: "btn btn-danger btn-sm" if can? :destroy, resource_class
