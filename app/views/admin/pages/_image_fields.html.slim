.nested-fields
  = f.input :title, :label => 'Título'
  = f.input :image, as: :image_preview, :label => 'Imagem', :input_html => { :preview_version => :preview }
  = link_to_remove_association "remover imagem", f
