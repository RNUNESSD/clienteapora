h2 Conteúdo - Categorias
.panel.panel-default
  .table-responsive
    table.table.table-striped.b-t.b-light
      thead
        tr
          th.text-center Nome
          th.text-center Está Ativa?
          th.text-center(width=310) Ações
      tbody    
        - collection.each do |r|
          tr
            td.text-center= r.name
            td.text-center
              = render 'admin/shared/is_active', r: r
            td.text-center.links-actions
              = render 'admin/shared/actions', r: r
= render 'admin/shared/create'
